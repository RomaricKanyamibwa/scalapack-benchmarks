include SLmake.inc

SRCS=$(wildcard src/*.f90)
PROGS=$(patsubst src/%.f90,bin/%_openmpi,$(SRCS))

all: $(PROGS)

bin/%_openmpi: src/%.f90
	$(FC) $(FFLAGS) -o $@ $<  $(SCALAPACKLIB) $(LIBS) -g
clean:
	rm -f $(PROGS)
